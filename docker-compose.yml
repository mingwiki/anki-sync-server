version: "3"
services:
  app:
    restart: unless-stopped
    build: .
    container_name: anki
    image: mingwiki/anki-sync-server:v1.3
    ports:
      - "27701:27701"
    volumes:
      - /data/anki:/data